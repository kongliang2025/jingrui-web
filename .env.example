# MongoDB 连接配置
# 获取方式：MongoDB Atlas -> Database -> Connect -> Connection String
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/?retryWrites=true&w=majority

# 数据库名称（可自定义）
DB_NAME=property-evidence
